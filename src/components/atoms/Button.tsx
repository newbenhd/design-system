import React from 'react';

interface IProps {
    name: string;
    size?: 'small' | 'medium' | 'larg';
    type?: 'primary' | 'stanard' | 'error';
}
export default (props: IProps) => <button>
    
</button>